/**
 * @file Simplest possible layer-pack entry point.
 *
 * Imported via the App/ namespace — layer-pack resolves "App/index"
 * to this file based on the rootFolder in .layers.json.
 */
console.log('Hello from layer-pack!');
