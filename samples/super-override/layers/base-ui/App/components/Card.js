/**
 * @file Base Card — inherited as-is when child doesn't override it.
 *
 * Since no child layer provides App/components/Card.js, this file
 * is used directly when importing "App/components/Card".
 */
module.exports = { shadow: true, padding: 16 };
