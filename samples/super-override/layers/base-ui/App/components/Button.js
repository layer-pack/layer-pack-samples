/**
 * @file Base Button — can be overridden by child layers via $super.
 *
 * This is the parent layer's Button component. Child projects can import
 * this via $super and extend or replace its exports.
 */
module.exports = { label: "Base Button", color: "gray", rounded: false };
