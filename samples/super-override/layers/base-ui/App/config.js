/**
 * @file Base config — child layers extend this via $super.
 *
 * Provides sensible defaults that child projects can override
 * while still inheriting values they don't change.
 */
module.exports = { theme: "light", version: 1, debug: false };
